(() => {
  'use strict';
  const S = window.StudioPro;
  if (!S) throw new Error('StudioPro core is missing');
  let pointerMode = null, pointerStart = null, pointerElementStart = null, mobilePanel = null, saveDebounce = null;
  const scheduleSave=(delay=180)=>{clearTimeout(saveDebounce);saveDebounce=setTimeout(()=>S.saveState(),delay);};
  function renderBackgroundGallery(){const gallery=document.getElementById('bgGallery');gallery.innerHTML='';S.backgroundPresets.forEach(preset=>{const btn=document.createElement('button');btn.type='button';btn.className='bg-swatch';btn.dataset.bg=preset.id;btn.title=preset.name;btn.style.background=`linear-gradient(${preset.angle}deg, ${preset.colors.join(', ')})`;btn.addEventListener('click',()=>S.selectBackground(preset.id));gallery.appendChild(btn);});S.syncBackgroundUI();}
  function activateSection(name){document.querySelectorAll('.tool-section').forEach(section=>section.classList.toggle('active',section.dataset.section===name));document.querySelectorAll('.panel-tab').forEach(tab=>tab.classList.toggle('active',tab.dataset.panel===name));const titleMap={design:'التصميم',background:'الخلفية',elements:'العناصر',localization:'الترجمة'},title=document.getElementById('mobilePanelTitle');if(title)title.textContent=titleMap[name]||'الأدوات';}
  function closeMobilePanels(){document.getElementById('leftPanel').classList.remove('mobile-open');document.getElementById('inspectorPanel').classList.remove('mobile-open');mobilePanel=null;document.querySelectorAll('.mobile-tool').forEach(btn=>btn.classList.remove('active'));setTimeout(()=>S.fitCanvasToStage(),250);}
  function openMobilePanel(name){if(window.innerWidth>780)return;const left=document.getElementById('leftPanel'),inspector=document.getElementById('inspectorPanel');if(mobilePanel===name&&(left.classList.contains('mobile-open')||inspector.classList.contains('mobile-open'))){closeMobilePanels();return;}left.classList.remove('mobile-open');inspector.classList.remove('mobile-open');if(name==='inspector')inspector.classList.add('mobile-open');else{activateSection(name);left.classList.add('mobile-open');}mobilePanel=name;document.querySelectorAll('.mobile-tool').forEach(btn=>btn.classList.toggle('active',btn.dataset.mobilePanel===name));}
  async function addImages(files){const list=[...files];for(const file of list){try{const img=await S.loadImageFile(file);S.addImage(img,file.name.replace(/\.[^.]+$/,''));}catch(err){S.showToast(err.message||'تعذر إضافة الصورة.');}}if(list.length&&window.innerWidth<=780)closeMobilePanels();}
  function canvasPointFromEvent(e){const rect=S.canvas.getBoundingClientRect();return{x:(e.clientX-rect.left)*S.canvas.width/rect.width,y:(e.clientY-rect.top)*S.canvas.height/rect.height};}
  function hitTestElement(el,point){const cx=S.canvas.width/2+el.x,cy=S.canvas.height/2+el.y,dx=point.x-cx,dy=point.y-cy,r=-(el.rotation||0)*Math.PI/180,lx=dx*Math.cos(r)-dy*Math.sin(r),ly=dx*Math.sin(r)+dy*Math.cos(r);let w,h;if(el.type==='image'){w=(el.baseWidth||400)*(el.scale||1);h=(el.baseHeight||800)*(el.scale||1);}else{const m=S.getTextMetrics(el);w=m.width;h=m.height;}return Math.abs(lx)<=w/2+16&&Math.abs(ly)<=h/2+16;}
  function topHit(point){for(let i=S.state.elements.length-1;i>=0;i--)if(hitTestElement(S.state.elements[i],point))return S.state.elements[i];return null;}
  function bindPointerEditing(){S.canvas.addEventListener('pointerdown',e=>{e.preventDefault();S.canvas.setPointerCapture?.(e.pointerId);const hit=topHit(canvasPointFromEvent(e));if(!hit){S.setActiveElement(null);return;}S.setActiveElement(hit.id);pointerMode=e.shiftKey?'rotate':'move';pointerStart={x:e.clientX,y:e.clientY};pointerElementStart={x:hit.x,y:hit.y,rotation:hit.rotation||0};});S.canvas.addEventListener('pointermove',e=>{if(!pointerMode||!pointerStart)return;const el=S.getActiveElement();if(!el)return;const rect=S.canvas.getBoundingClientRect(),dx=(e.clientX-pointerStart.x)*S.canvas.width/rect.width,dy=(e.clientY-pointerStart.y)*S.canvas.height/rect.height;if(pointerMode==='rotate')el.rotation=pointerElementStart.rotation+dx*.24;else{el.x=pointerElementStart.x+dx;el.y=pointerElementStart.y+dy;}if(el.type==='text'&&el.autoFit)S.fitTextElementToCanvas(el);S.render();S.syncAllUI();});const finish=()=>{if(!pointerMode)return;pointerMode=null;pointerStart=null;pointerElementStart=null;S.saveState();};S.canvas.addEventListener('pointerup',finish);S.canvas.addEventListener('pointercancel',finish);S.canvas.addEventListener('dblclick',e=>{const hit=topHit(canvasPointFromEvent(e));if(hit?.type==='text'){S.setActiveElement(hit.id);if(window.innerWidth<=780)openMobilePanel('inspector');document.getElementById('textContent').focus();}});}
  function bindKeyboard(){document.addEventListener('keydown',e=>{const tag=document.activeElement?.tagName,editing=tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT';if((e.ctrlKey||e.metaKey)&&!e.shiftKey&&e.code==='KeyZ'){e.preventDefault();S.undo();return;}if((e.ctrlKey||e.metaKey)&&e.shiftKey&&e.code==='KeyZ'){e.preventDefault();S.redo();return;}if(editing)return;const el=S.getActiveElement();if((e.key==='Delete'||e.key==='Backspace')&&el){e.preventDefault();S.deleteElement(el.id);return;}if(e.key==='Escape'){S.setActiveElement(null);return;}if(!el)return;if(e.key.toLowerCase()==='t'&&S.state.elements.length){const idx=S.state.elements.findIndex(x=>x.id===el.id);S.setActiveElement(S.state.elements[(idx+1)%S.state.elements.length].id);return;}if(e.altKey&&(e.key==='ArrowUp'||e.key==='ArrowDown')){e.preventDefault();el.scale=Math.max(.1,el.scale+(e.key==='ArrowUp'?.04:-.04));S.render();S.syncAllUI();scheduleSave();return;}if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight'].includes(e.key)){e.preventDefault();const step=e.shiftKey?40:8;if(e.key==='ArrowUp')el.y-=step;if(e.key==='ArrowDown')el.y+=step;if(e.key==='ArrowLeft')el.x-=step;if(e.key==='ArrowRight')el.x+=step;if(el.type==='text'&&el.autoFit)S.fitTextElementToCanvas(el);S.render();S.syncAllUI();scheduleSave();}});}
  function bindInspector(){const onActive=fn=>e=>{const el=S.getActiveElement();if(!el)return;fn(el,e);S.render();S.syncAllUI();scheduleSave();};document.getElementById('textContent').addEventListener('input',onActive((el,e)=>{if(el.type==='text'){el.content=e.target.value;if(el.autoFit)S.fitTextElementToCanvas(el);}}));document.getElementById('fontSize').addEventListener('input',onActive((el,e)=>{if(el.type==='text'){el.fontSize=Math.max(12,+e.target.value||12);if(el.autoFit)S.fitTextElementToCanvas(el,{startFontSize:el.fontSize});}}));document.getElementById('textColor').addEventListener('input',onActive((el,e)=>{if(el.type==='text')el.color=e.target.value;}));document.getElementById('textAutoFit').addEventListener('change',onActive((el,e)=>{if(el.type==='text'){el.autoFit=e.target.checked;if(el.autoFit)S.fitTextElementToCanvas(el);}}));document.getElementById('translationKey').addEventListener('change',e=>{const el=S.getActiveElement();if(!el||el.type!=='text')return;const key=e.target.value.trim().replace(/[^a-zA-Z0-9_-]/g,'_')||el.translationKey,duplicate=S.getTextElements().some(x=>x.id!==el.id&&x.translationKey===key);if(duplicate){e.target.value=el.translationKey;S.showToast('معرّف الترجمة مستخدم لنص آخر.');return;}el.translationKey=key;e.target.value=key;S.syncAllUI();S.saveState();});document.getElementById('elementScale').addEventListener('input',onActive((el,e)=>{el.scale=Math.max(.1,+e.target.value/100);if(el.type==='text'&&el.autoFit)S.fitTextElementToCanvas(el);}));document.getElementById('elementRotation').addEventListener('input',onActive((el,e)=>{el.rotation=+e.target.value||0;if(el.type==='text'&&el.autoFit)S.fitTextElementToCanvas(el);}));document.getElementById('borderRadiusSlider').addEventListener('input',onActive((el,e)=>{if(el.type==='image')el.borderRadius=+e.target.value;}));document.getElementById('frameColorPicker').addEventListener('input',onActive((el,e)=>{if(el.type==='image')el.frameColor=e.target.value;}));document.querySelectorAll('#deviceGrid [data-frame]').forEach(btn=>btn.addEventListener('click',()=>{const el=S.getActiveElement();if(!el||el.type!=='image')return;el.frame=btn.dataset.frame;S.render();S.syncAllUI();S.saveState();}));document.getElementById('shadowEnabled').addEventListener('change',onActive((el,e)=>{el.shadow=el.shadow||S.makeShadow(el.type);el.shadow.enabled=e.target.checked;}));document.getElementById('shadowBlur').addEventListener('input',onActive((el,e)=>{el.shadow.blur=+e.target.value;}));document.getElementById('shadowOpacity').addEventListener('input',onActive((el,e)=>{el.shadow.opacity=+e.target.value;}));document.getElementById('shadowX').addEventListener('input',onActive((el,e)=>{el.shadow.x=+e.target.value;}));document.getElementById('shadowY').addEventListener('input',onActive((el,e)=>{el.shadow.y=+e.target.value;}));document.getElementById('shadowColor').addEventListener('input',onActive((el,e)=>{el.shadow.color=e.target.value;}));document.getElementById('deleteInspectorBtn').addEventListener('click',()=>{const el=S.getActiveElement();if(el)S.deleteElement(el.id);});document.getElementById('centerXBtn').addEventListener('click',()=>S.centerElement('x'));document.getElementById('centerYBtn').addEventListener('click',()=>S.centerElement('y'));}
  function bindCanvasAndBackgroundControls(){const imageInput=document.getElementById('imageInput');document.getElementById('addImageBtn').addEventListener('click',()=>imageInput.click());document.getElementById('emptyAddImage').addEventListener('click',()=>imageInput.click());imageInput.addEventListener('change',async e=>{await addImages(e.target.files);e.target.value='';});document.getElementById('addTextBtn').addEventListener('click',()=>{S.addText('نص جديد');if(window.innerWidth<=780)openMobilePanel('inspector');});document.getElementById('deleteBtn').addEventListener('click',()=>{const el=S.getActiveElement();if(el)S.deleteElement(el.id);});document.querySelectorAll('.preset-btn[data-size]').forEach(btn=>btn.addEventListener('click',()=>{const[w,h]=btn.dataset.size.split('x').map(Number);S.setCanvasSize(w,h);}));document.getElementById('applyCustomSize').addEventListener('click',()=>{const w=Math.max(320,Math.min(7680,+document.getElementById('customWidth').value||1080)),h=Math.max(320,Math.min(7680,+document.getElementById('customHeight').value||1920));S.setCanvasSize(w,h);});document.getElementById('zoomIn').addEventListener('click',()=>S.setZoom(S.viewState.zoom+10));document.getElementById('zoomOut').addEventListener('click',()=>S.setZoom(S.viewState.zoom-10));document.getElementById('zoomReset').addEventListener('click',()=>S.setZoom(100));document.getElementById('centerElementBtn').addEventListener('click',()=>S.centerElement('both'));document.getElementById('fitElementBtn').addEventListener('click',()=>S.fitElementToCanvas());const solid=document.getElementById('solidColor'),hex=document.getElementById('solidHex');solid.addEventListener('input',()=>{hex.value=solid.value.toUpperCase();S.useSolidBackground(solid.value);});hex.addEventListener('change',()=>{const v=hex.value.trim();if(/^#[0-9a-f]{6}$/i.test(v)){solid.value=v;S.useSolidBackground(v);}else hex.value=solid.value.toUpperCase();});document.getElementById('smartBgBtn').addEventListener('click',()=>S.suggestBackgroundFromImage());const bgInput=document.getElementById('bgImageInput');document.getElementById('bgImageBtn').addEventListener('click',()=>bgInput.click());bgInput.addEventListener('change',async e=>{const file=e.target.files[0];if(!file)return;try{S.useImageBackground(await S.loadImageFile(file));}catch(err){S.showToast(err.message);}e.target.value='';});document.getElementById('bgBlur').addEventListener('input',e=>{S.state.background.blur=+e.target.value;S.render();scheduleSave();});document.getElementById('bgOverlay').addEventListener('input',e=>{S.state.background.overlay=+e.target.value;S.render();scheduleSave();});document.getElementById('exportBtn').addEventListener('click',()=>S.exportCanvas());}
  function bindNavigation(){document.querySelectorAll('.panel-tab').forEach(tab=>tab.addEventListener('click',()=>activateSection(tab.dataset.panel)));document.querySelectorAll('.mobile-tool').forEach(btn=>btn.addEventListener('click',()=>openMobilePanel(btn.dataset.mobilePanel)));document.getElementById('closeMobilePanel').addEventListener('click',closeMobilePanels);document.getElementById('closeInspectorPanel').addEventListener('click',closeMobilePanels);window.addEventListener('resize',()=>{if(window.innerWidth>780){closeMobilePanels();document.querySelector('.mobile-tool[data-mobile-panel="design"]')?.classList.add('active');}S.fitCanvasToStage();});}
  renderBackgroundGallery();bindNavigation();bindCanvasAndBackgroundControls();bindInspector();bindPointerEditing();bindKeyboard();document.getElementById('undoBtn').addEventListener('click',S.undo);document.getElementById('redoBtn').addEventListener('click',S.redo);activateSection('design');S.syncAllUI();
})();