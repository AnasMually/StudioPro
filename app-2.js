(() => {
  'use strict';
  const S=window.StudioPro,{canvas,ctx,state,viewState}=S;
  const grad=(colors,angle=135)=>{const r=angle*Math.PI/180,x=Math.cos(r),y=Math.sin(r),cx=canvas.width/2,cy=canvas.height/2,len=Math.abs(canvas.width*x)+Math.abs(canvas.height*y),g=ctx.createLinearGradient(cx-x*len/2,cy-y*len/2,cx+x*len/2,cy+y*len/2);(colors?.length?colors:['#fff','#f3f4f8']).forEach((c,i,a)=>g.addColorStop(a.length===1?0:i/(a.length-1),c));return g;};
  const spot=(color,a,nx,ny)=>{const x=canvas.width*nx,y=canvas.height*ny,r=Math.max(canvas.width,canvas.height)*.42,g=ctx.createRadialGradient(x,y,0,x,y,r),rgb=S.hexToRgb(color);g.addColorStop(0,`rgba(${rgb.r},${rgb.g},${rgb.b},${a})`);g.addColorStop(1,`rgba(${rgb.r},${rgb.g},${rgb.b},0)`);ctx.fillStyle=g;ctx.fillRect(0,0,canvas.width,canvas.height);};
  S.renderBackground=()=>{ctx.clearRect(0,0,canvas.width,canvas.height);const b=state.background;if(b.type==='solid'){ctx.fillStyle=b.solid||'#f3f4f8';ctx.fillRect(0,0,canvas.width,canvas.height);return;}if(b.type==='image'&&b.image){const sc=Math.max(canvas.width/b.image.width,canvas.height/b.image.height),w=b.image.width*sc,h=b.image.height*sc;ctx.save();ctx.filter=b.blur?`blur(${b.blur}px)`:'none';ctx.drawImage(b.image,(canvas.width-w)/2,(canvas.height-h)/2,w,h);ctx.restore();if(b.overlay>0){ctx.fillStyle=`rgba(255,255,255,${b.overlay/100})`;ctx.fillRect(0,0,canvas.width,canvas.height);}return;}const p=S.backgroundPresets.find(x=>x.id===b.presetId)||S.backgroundPresets[0],colors=b.type==='smart'&&b.colors?b.colors:p.colors;ctx.fillStyle=grad(colors,p.angle);ctx.fillRect(0,0,canvas.width,canvas.height);(b.type==='smart'?(b.spots||[]):p.spots).forEach(x=>spot(...x));};
  S.imageSize=el=>({w:(el.baseWidth||400)*(el.scale||1),h:(el.baseHeight||800)*(el.scale||1)});
  S.getTextMetrics=(el,fontSize=el.fontSize)=>{ctx.save();ctx.font=`700 ${fontSize}px 'Cairo',system-ui,sans-serif`;const lines=String(el.content||'').split('\n'),width=Math.max(1,...lines.map(x=>ctx.measureText(x||' ').width))*(el.scale||1),lh=fontSize*1.25*(el.scale||1);ctx.restore();return{lines,width,height:Math.max(lh,lines.length*lh),lineHeight:lh};};
  const rb=(w,h,d)=>{const r=Math.abs(d||0)*Math.PI/180,c=Math.abs(Math.cos(r)),s=Math.abs(Math.sin(r));return{width:w*c+h*s,height:w*s+h*c};};
  S.fitTextElementToCanvas=(el,opt={})=>{if(!el||el.type!=='text'||el.autoFit===false)return;const m=Math.max(28,Math.min(canvas.width,canvas.height)*.045);let cx=canvas.width/2+el.x,cy=canvas.height/2+el.y;cx=Math.max(m,Math.min(canvas.width-m,cx));cy=Math.max(m,Math.min(canvas.height-m,cy));el.x=cx-canvas.width/2;el.y=cy-canvas.height/2;const mw=Math.max(80,2*Math.min(cx-m,canvas.width-m-cx)),mh=Math.max(80,2*Math.min(cy-m,canvas.height-m-cy));el.fontSize=opt.startFontSize||el.fontSize;let g=0;while(el.fontSize>(opt.minFontSize||14)&&g++<180){const q=S.getTextMetrics(el,el.fontSize),b=rb(q.width,q.height,el.rotation);if(b.width<=mw&&b.height<=mh)break;const ratio=Math.min(mw/b.width,mh/b.height),n=Math.max(opt.minFontSize||14,Math.floor(el.fontSize*Math.min(.96,ratio*.98)));el.fontSize=n===el.fontSize?el.fontSize-1:n;}};
  const path=(x,y,w,h,r)=>{r=Math.min(Math.max(0,r),Math.abs(w)/2,Math.abs(h)/2);ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.quadraticCurveTo(x+w,y,x+w,y+r);ctx.lineTo(x+w,y+h-r);ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);ctx.lineTo(x+r,y+h);ctx.quadraticCurveTo(x,y+h,x,y+h-r);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath();};
  const shadow=el=>{const s=el.shadow||{};if(!s.enabled||s.opacity<=0||s.blur<=0){ctx.shadowColor='transparent';ctx.shadowBlur=0;ctx.shadowOffsetX=ctx.shadowOffsetY=0;return;}ctx.shadowColor=S.rgba(s.color||'#111827',s.opacity??30);ctx.shadowBlur=s.blur||0;ctx.shadowOffsetX=s.x||0;ctx.shadowOffsetY=s.y||0;};
  const selection=(el,w,h)=>{if(!viewState.showSelection||el.id!==state.activeElementId)return;ctx.save();ctx.shadowColor='transparent';ctx.strokeStyle='#5b5ce2';ctx.lineWidth=3;ctx.setLineDash([10,7]);ctx.strokeRect(-w/2-8,-h/2-8,w+16,h+16);ctx.restore();};
  const drawImage=el=>{const{w,h}=S.imageSize(el),f=el.frame||'none',r=f==='rounded'?(el.borderRadius??40):f==='none'?0:(el.borderRadius??36);if(f==='none'){shadow(el);ctx.drawImage(el.image,-w/2,-h/2,w,h);ctx.shadowColor='transparent';selection(el,w,h);return;}shadow(el);ctx.fillStyle=el.frameColor||'#111827';path(-w/2-8,-h/2-8,w+16,h+16,r+8);ctx.fill();ctx.shadowColor='transparent';ctx.save();path(-w/2,-h/2,w,h,r);ctx.clip();ctx.drawImage(el.image,-w/2,-h/2,w,h);ctx.restore();ctx.strokeStyle=el.frameColor||'#111827';ctx.lineWidth=f==='rounded'?4:7;path(-w/2,-h/2,w,h,r);ctx.stroke();if(f==='iphone'){ctx.fillStyle=el.frameColor||'#111827';path(-w*.12,-h/2+9,w*.24,20,10);ctx.fill();}else if(f==='samsung'){ctx.fillStyle=el.frameColor||'#111827';ctx.beginPath();ctx.arc(0,-h/2+15,6,0,Math.PI*2);ctx.fill();}selection(el,w+16,h+16);};
  const drawText=el=>{const m=S.getTextMetrics({...el,scale:1});ctx.font=`700 ${el.fontSize}px 'Cairo',system-ui,sans-serif`;ctx.fillStyle=el.color||'#111827';ctx.textAlign='center';ctx.textBaseline='middle';ctx.direction=S.detectDirection(el.content);shadow(el);m.lines.forEach((line,i)=>ctx.fillText(line,0,i*m.lineHeight-(m.lines.length-1)*m.lineHeight/2));ctx.shadowColor='transparent';selection(el,m.width,m.height);};
  S.render=()=>{S.renderBackground();state.elements.forEach(el=>{ctx.save();ctx.translate(canvas.width/2+el.x,canvas.height/2+el.y);ctx.rotate((el.rotation||0)*Math.PI/180);if(el.type==='image')drawImage(el);else{ctx.scale(el.scale||1,el.scale||1);drawText({...el,scale:1});}ctx.restore();});document.getElementById('emptyState')?.classList.toggle('hidden',state.elements.length>0);};
  S.fitCanvasToStage=()=>{const rect=S.canvasArea.getBoundingClientRect();if(!rect.width||!rect.height)return;const pad=window.innerWidth<=780?18:34,base=Math.min((rect.width-pad)/canvas.width,(rect.height-pad)/canvas.height),sc=Math.max(.03,base*(viewState.zoom/100));canvas.style.width=`${Math.round(canvas.width*sc)}px`;canvas.style.height=`${Math.round(canvas.height*sc)}px`;const z=document.getElementById('zoomDisplay');if(z)z.textContent=`${Math.round(viewState.zoom)}%`;};
  S.setZoom=v=>{viewState.zoom=Math.max(30,Math.min(220,v));S.fitCanvasToStage();};
  S.setCanvasSize=(w,h)=>{const sx=w/state.canvas.width,sy=h/state.canvas.height;state.elements.forEach(el=>{el.x*=sx;el.y*=sy;});state.canvas={width:w,height:h};canvas.width=w;canvas.height=h;S.getTextElements().forEach(S.fitTextElementToCanvas);S.render();S.fitCanvasToStage();S.syncAllUI();S.saveState();};
  S.loadImageFile=f=>new Promise((res,rej)=>{const r=new FileReader();r.onerror=()=>rej(new Error('تعذر قراءة الصورة'));r.onload=e=>{const i=new Image();i.onload=()=>res(i);i.onerror=()=>rej(new Error('ملف الصورة غير صالح'));i.src=e.target.result;};r.readAsDataURL(f);});
  S.addImage=(img,name='صورة')=>{const ratio=Math.min(canvas.width*.68/img.width,canvas.height*.68/img.height,1),el={id:S.uid('img'),type:'image',name:name||'صورة',image:img,x:0,y:0,scale:1,rotation:0,baseWidth:Math.max(20,img.width*ratio),baseHeight:Math.max(20,img.height*ratio),frame:'none',frameColor:'#111827',borderRadius:46,shadow:S.makeShadow('image')};state.elements.push(el);state.activeElementId=el.id;S.render();S.syncAllUI();S.saveState();return el;};
  S.addText=(content='نص جديد')=>{const el={id:S.uid('txt'),type:'text',name:'نص',content,x:0,y:-canvas.height*.22,scale:1,rotation:0,fontSize:Math.round(Math.min(canvas.width,canvas.height)*.065),color:'#111827',autoFit:true,translationKey:S.uniqueTranslationKey(),shadow:{enabled:false,blur:16,opacity:24,x:0,y:8,color:'#111827'}};state.elements.push(el);state.activeElementId=el.id;S.fitTextElementToCanvas(el);S.render();S.syncAllUI();S.saveState();return el;};
  S.deleteElement=id=>{const i=state.elements.findIndex(x=>x.id===id);if(i<0)return;state.elements.splice(i,1);if(state.activeElementId===id)state.activeElementId=state.elements[i-1]?.id||state.elements[i]?.id||null;S.render();S.syncAllUI();S.saveState();};
  S.setActiveElement=id=>{state.activeElementId=id;S.render();S.syncAllUI();};
  S.centerElement=(axis='both',el=S.getActiveElement())=>{if(!el)return;if(axis==='both'||axis==='x')el.x=0;if(axis==='both'||axis==='y')el.y=0;S.render();S.syncAllUI();S.saveState();};
  S.fitElementToCanvas=(el=S.getActiveElement())=>{if(!el)return;if(el.type==='text')S.fitTextElementToCanvas(el);else{const q=S.imageSize(el),mw=canvas.width*.82,mh=canvas.height*.82;if(q.w>mw||q.h>mh)el.scale*=Math.min(mw/q.w,mh/q.h);}el.x=Math.max(-canvas.width*.45,Math.min(canvas.width*.45,el.x));el.y=Math.max(-canvas.height*.45,Math.min(canvas.height*.45,el.y));S.render();S.syncAllUI();S.saveState();};
  S.selectBackground=id=>{state.background.type='preset';state.background.presetId=id;state.background.colors=null;S.render();S.syncAllUI();S.saveState();};
  S.useSolidBackground=h=>{state.background.type='solid';state.background.solid=h;S.render();S.syncAllUI();S.saveState();};
  S.useImageBackground=i=>{state.background.type='image';state.background.image=i;S.render();S.syncAllUI();S.saveState();};
  const rgbHsl=(r,g,b)=>{r/=255;g/=255;b/=255;const mx=Math.max(r,g,b),mn=Math.min(r,g,b);let h=0,s=0,l=(mx+mn)/2;if(mx!==mn){const d=mx-mn;s=l>.5?d/(2-mx-mn):d/(mx+mn);if(mx===r)h=(g-b)/d+(g<b?6:0);else if(mx===g)h=(b-r)/d+2;else h=(r-g)/d+4;h*=60;}return{h,s:s*100,l:l*100};};
  const hslHex=(h,s,l)=>{s/=100;l/=100;const c=(1-Math.abs(2*l-1))*s,x=c*(1-Math.abs((h/60)%2-1)),m=l-c/2;let r=0,g=0,b=0;if(h<60){r=c;g=x}else if(h<120){r=x;g=c}else if(h<180){g=c;b=x}else if(h<240){g=x;b=c}else if(h<300){r=x;b=c}else{r=c;b=x}return'#'+[r,g,b].map(v=>Math.round((v+m)*255).toString(16).padStart(2,'0')).join('');};
  S.suggestBackgroundFromImage=()=>{const el=state.elements.find(x=>x.type==='image');if(!el){S.showToast('أضف صورة أولًا لاستخراج ألوان مناسبة.');return;}const o=document.createElement('canvas');o.width=o.height=64;const c=o.getContext('2d',{willReadFrequently:true});c.drawImage(el.image,0,0,64,64);const d=c.getImageData(0,0,64,64).data;let ar=0,ag=0,ab=0,n=0,b1={score:-1,r:91,g:92,b:226},b2={score:-1,r:129,g:140,b:248};for(let i=0;i<d.length;i+=16){const r=d[i],g=d[i+1],b=d[i+2],a=d[i+3];if(a<80)continue;ar+=r;ag+=g;ab+=b;n++;const h=rgbHsl(r,g,b),sc=h.s*(1-Math.abs(h.l-55)/70);if(sc>b1.score){b2=b1;b1={score:sc,r,g,b}}else if(sc>b2.score)b2={score:sc,r,g,b};}if(!n)return;const av=rgbHsl(ar/n,ag/n,ab/n),a=rgbHsl(b1.r,b1.g,b1.b),z=rgbHsl(b2.r,b2.g,b2.b);state.background.type='smart';state.background.colors=[hslHex(a.h,Math.min(58,Math.max(22,a.s)),93),hslHex(av.h,Math.min(42,Math.max(14,av.s)),97),hslHex(z.h,Math.min(54,Math.max(20,z.s)),91)];state.background.spots=[[hslHex(a.h,Math.min(70,a.s+10),68),.15,.18,.22],[hslHex(z.h,Math.min(70,z.s+10),70),.14,.82,.78]];state.background.presetId='cloud';S.render();S.syncAllUI();S.saveState();S.showToast('تم إنشاء خلفية متناسقة من ألوان الصورة.');};
  S.exportCanvas=(name=`studiopro_${Date.now()}.png`)=>{const a=state.activeElementId;state.activeElementId=null;S.render();const link=document.createElement('a');link.download=name;link.href=canvas.toDataURL('image/png',1);link.click();state.activeElementId=a;S.render();};
})();